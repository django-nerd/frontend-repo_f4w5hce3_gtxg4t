import React from 'react'
import { AppShell } from '../components/layout/AppShell'

export const NotFound: React.FC = () => (
  <AppShell>
    <h1>Page not found</h1>
    <p>We couldn’t find that page. Try the catalog or go back home.</p>
  </AppShell>
)
